version: "3.9"

services:
  order-api:
    build: .
    restart: unless-stopped
    ports:
      - "8001:8000"
    volumes:
      - .:/app
    networks:
      - app-net

networks:
  app-net:
    external: true
    name: app-net