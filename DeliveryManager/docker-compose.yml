version: "3.9"

services:
  delmanager:
    build: .
    restart: unless-stopped
    ports:
      - "8002:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    networks:
      - app-net

networks:
  app-net:
    external: true
    name: app-net