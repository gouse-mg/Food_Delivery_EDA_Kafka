version: "3.9"

services:
  route-api:
    build: .
    restart: unless-stopped
    ports:
      - "8003:8000"
    volumes:
      - .:/app
    networks:
      - app-net

networks:
  app-net:
    external: true
    name: app-net
